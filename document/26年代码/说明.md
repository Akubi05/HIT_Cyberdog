**此为最小框架了**
已实现 识别 各种颜色球、蓝色方块、虚线、可乐（援助）、道路居中算法
已实现 用IMU指定角度旋转

**现在建议按q退出程序而不是ctrl C**

把之前所有的全局变量全部移动到类里面了

优化了main的逻辑

---

以下说明如果你对之前的代码比较熟悉，一看就明白了。

# visionnode类

此为对应状态需要的检测函数，如果不需要检测就不添加
```
self.detectors = {
    0: self._detect_0,
    1: self._detect_1,
    3: self._detect_3,
    4: self._detect_4,
    4.5: self._detect_4_5,
    5: self._detect_blue_cube,
    6: self._detect_dotted_line,
    'boll': self._detect_boll,
    'cola': self._detect_cola,
    'blue_cube': self._detect_blue_cube,
    'dotted_line': self._detect_dotted_line,
    'lane_center': self._detect_lane_center,
}
self.preview_states = {2, 7, 11, 17, 31, 44, 48, 53}

self._detected_flags = {
    '0': False,
    '1': False,
    '2': False,
    '3': False,
    '4': False,
    '4_5': False,
    '5': False,
    '6': False,
    '8': False,
    '9': False,
    '10': False,
    '12': False,
    '13': False,

    'boll': False,
    'cola': False,
    'blue_cube':False,
    'dotted_line':False,
    'lane_center':False
        }
```
在这个类里添加检测逻辑
```
def _detect_boll 识别球
def _detect_cola(self, cv_image): 识别可乐
def _detect_blue_cube(self, cv_image): 识别蓝色块
def _detect_dotted_line(self, cv_image): 识别虚线
def _detect_lane_center 直道居中检测
```


# class RobotApp类

此为状态类，即原先的状态机，我改成了一个类

这个变量是状态机的状态，你需要添加什么状态就加什么状态
```
self.state_handlers = {
    0: self._handle_state_0,
    'boll': self._handle_state_boll,
    'cola': self._handle_state_cola,
    'dotted_line': self._handle_state_dotted_line,
    'blue_cube': self._handle_state_blue_cube,
    'lane_center': self._handle_state_lane_center,
}
```

_turn_by_yaw  IMU指定**角度旋转**



_handle_state_这一系列函数就是对应状态该执行的动作
```
def _handle_state_cola(self, current_time, last_cmd_time, cmd_interval):
    if self._tick_ready(current_time, last_cmd_time, cmd_interval):
        self._send_cmd(mode=21, gait_id=0)
        last_cmd_time = current_time
    if self.vision_node.is_detected('cola'):
        print('检测到可乐')
        self.app_state.state_id = 4
    return last_cmd_time
```

# 补充
原
```
@property
def _6_detected(self):
    with self._lock:
        return self._detected_flags['6']
```
这种我已全部删掉
换成
```
def is_detected(self, key):
    with self._lock:
        return self._detected_flags.get(key, False)
```
