VScode连接到WSL中，目录为/home/eric/cyberdog_compile
进入文件夹在cyberdog_compile目录下执行命令 docker run -it --rm --name alan -v /home/eric/cyberdog_compile/cyberdog_locomotion:/work/build_farm/workspace/cyberdog cr.d.xiaomi.net/athena/athena_cheetah_arm64:2.0 /bin/bash
在docker环境下执行下面命令：

cd /work/build_farm/workspace/cyberdog/
rm -r onboard-build/
mkdir onboard-build && cd onboard-build

cd /work/build_farm/workspace/cyberdog/onboard-build
rm -rf robot-software

chmod +x /work/build_farm/workspace/cyberdog/common/lcm_type/gen_hpp.sh

cmake -DCMAKE_TOOLCHAIN_FILE=/usr/xcc/aarch64-openwrt-linux-gnu/Toolchain.cmake -DONBOARD_BUILD=ON -DBUILD_FACTORY=ON -DBUILD_CYBERDOG2=ON ..
make -j16

ssh-keygen -f "/home/eric/.ssh/known_hosts" -R "192.168.44.1"
sudo ssh-keygen -f "/root/.ssh/known_hosts" -R "192.168.44.233"

之后将网线连接至电脑右端网口与机器狗网口（电脑端网口连接方法：将网线突出段压入右侧网口可开合的卡口，之后轻推，将网线送入以太网口）网线可能接触不良，用手摇晃按压机器狗端网口至网口旁边灯闪烁
进入locomotion文件夹下scripts目录
cd ~/cyberdog_compile/cyberdog_lo	comotion/scripts
sudo ./scp_to_cyberdog.sh
出现下述提示证明烧录成功
+ ssh root@192.168.44.233 sync
+ ssh root@192.168.44.233 'export LD_LIBRARY_PATH=/robot/robot-software/build;/robot/robot-software/manager /robot/ > /robot/manager.log 2>&1 &'

可以在下面已经打开的VScode中进行上述操作
如果需要打开新的终端，按win+R出现命令符提示框，输入cmd，弹出windows powershell
鼠标移至上边框最右侧小白倒三角，单击，出现下拉列表，选择Ubuntu 20.04.6 LTS，可像正常Ubuntu系统一样进行操作

如果需要远程连接，可以采用VScode，终端，MobaXterm三种方式，前两种方式此处不过多赘述，至于MobaXterm的使用方式，主要考验你的计统课程实验完成度

电脑密码：1208
WSL中Ubuntu密码：123456